# UEFI 新人訓練 — 第一個作業學習筆記

**日期：** 2026-05-12  
**作業目標：** 編譯並執行第一個 UEFI 應用程式 `MyHello.efi`

---

## 一、整體概念

UEFI 程式不像一般 Linux 程式可以直接執行，它需要跑在 UEFI 環境裡。  
我們的做法是：

```
寫 C 程式碼
    ↓
用 EDK2 框架編譯成 .efi 檔
    ↓
放進 FAT32 虛擬磁碟
    ↓
用 QEMU + OVMF 模擬 UEFI 環境
    ↓
在 UEFI Shell 執行 .efi
```

---

## 二、專案目錄結構

```
~/edk2/
└── MyPkg/                  ← 自己建的 Package（模組集合）
    ├── MyPkg.dsc           ← Package 描述檔（整個專案的設定）
    └── MyHello/
        ├── MyHello.c       ← 實際的 C 程式碼
        └── MyHello.inf     ← 模組描述檔（這個程式的設定）
```

---

## 三、各檔案說明

### 1. `MyHello.c` — 主程式

```c
#include <Uefi.h>
#include <Library/UefiApplicationEntryPoint.h>
#include <Library/UefiLib.h>

EFI_STATUS
EFIAPI
UefiMain (
  IN EFI_HANDLE        ImageHandle,
  IN EFI_SYSTEM_TABLE  *SystemTable
  )
{
  Print(L"Hello UEFI\n");
  return EFI_SUCCESS;
}
```

| 程式碼 | 說明 |
|--------|------|
| `#include <Uefi.h>` | 引入 UEFI 基本型別定義，例如 `EFI_STATUS`、`EFI_HANDLE` |
| `#include <Library/UefiApplicationEntryPoint.h>` | 提供 UEFI 應用程式的進入點包裝，讓 EDK2 知道從哪裡開始執行 |
| `#include <Library/UefiLib.h>` | 提供常用工具函式，包含 `Print()` |
| `EFI_STATUS` | UEFI 函式的回傳型別，類似 Linux 的 `int`，用來表示成功或錯誤碼 |
| `EFIAPI` | 呼叫慣例修飾詞（calling convention），確保跨平台函式介面一致 |
| `UefiMain` | 程式進入點，相當於一般 C 程式的 `main()` |
| `IN EFI_HANDLE ImageHandle` | 這個程式本身被載入後的 handle，可用來取得程式自身資訊 |
| `IN EFI_SYSTEM_TABLE *SystemTable` | 指向 UEFI System Table 的指標，是存取 UEFI 所有服務的入口 |
| `Print(L"Hello UEFI\n")` | 印出文字到 console，`L""` 表示 Unicode 寬字元字串（UEFI 內部全部用 Unicode） |
| `return EFI_SUCCESS` | 回傳成功，值為 0 |

---

### 2. `MyHello.inf` — 模組描述檔

```ini
[Defines]
  INF_VERSION    = 0x00010006
  BASE_NAME      = MyHello
  FILE_GUID      = a1b2c3d4-e5f6-7890-abcd-ef1234567890
  MODULE_TYPE    = UEFI_APPLICATION
  VERSION_STRING = 1.0
  ENTRY_POINT    = UefiMain

[Sources]
  MyHello.c

[Packages]
  MdePkg/MdePkg.dec

[LibraryClasses]
  UefiApplicationEntryPoint
  UefiLib
```

| 欄位 | 說明 |
|------|------|
| `INF_VERSION` | INF 格式版本號，固定填 `0x00010006` |
| `BASE_NAME` | 編譯輸出的檔名，會產生 `MyHello.efi` |
| `FILE_GUID` | 這個模組的唯一識別碼，每個模組必須不同，格式為 UUID |
| `MODULE_TYPE = UEFI_APPLICATION` | 宣告這是一個 UEFI 應用程式（對應到可在 Shell 執行的 .efi）|
| `ENTRY_POINT = UefiMain` | 告訴 EDK2 程式進入點是 `UefiMain` 這個函式 |
| `[Sources]` | 列出所有要編譯的 .c 原始檔 |
| `[Packages]` | 宣告用到哪些 Package 的 .dec（Declaration）檔，才能找到 header |
| `[LibraryClasses]` | 宣告需要哪些 Library，實際路徑由 .dsc 決定 |

---

### 3. `MyPkg.dsc` — Package 描述檔

```ini
[Defines]
  PLATFORM_NAME           = MyPkg
  PLATFORM_GUID           = 12345678-1234-1234-1234-123456789abc
  PLATFORM_VERSION        = 1.0
  DSC_SPECIFICATION       = 0x00010006
  OUTPUT_DIRECTORY        = Build/MyPkg
  SUPPORTED_ARCHITECTURES = X64
  BUILD_TARGETS           = DEBUG|RELEASE

[LibraryClasses]
  UefiApplicationEntryPoint|MdePkg/Library/UefiApplicationEntryPoint/UefiApplicationEntryPoint.inf
  UefiLib|MdePkg/Library/UefiLib/UefiLib.inf
  UefiBootServicesTableLib|MdePkg/Library/UefiBootServicesTableLib/UefiBootServicesTableLib.inf
  UefiRuntimeServicesTableLib|MdePkg/Library/UefiRuntimeServicesTableLib/UefiRuntimeServicesTableLib.inf
  BaseLib|MdePkg/Library/BaseLib/BaseLib.inf
  BaseMemoryLib|MdePkg/Library/BaseMemoryLib/BaseMemoryLib.inf
  DebugLib|MdePkg/Library/BaseDebugLibNull/BaseDebugLibNull.inf
  PrintLib|MdePkg/Library/BasePrintLib/BasePrintLib.inf
  DevicePathLib|MdePkg/Library/UefiDevicePathLib/UefiDevicePathLib.inf
  RegisterFilterLib|MdePkg/Library/RegisterFilterLibNull/RegisterFilterLibNull.inf
  PcdLib|MdePkg/Library/BasePcdLibNull/BasePcdLibNull.inf
  MemoryAllocationLib|MdePkg/Library/UefiMemoryAllocationLib/UefiMemoryAllocationLib.inf
  StackCheckLib|MdePkg/Library/StackCheckLibNull/StackCheckLibNull.inf

[Components]
  MyPkg/MyHello/MyHello.inf
```

| 欄位 | 說明 |
|------|------|
| `OUTPUT_DIRECTORY` | 編譯輸出目錄，最終 .efi 會在 `Build/MyPkg/DEBUG_GCC/X64/` |
| `SUPPORTED_ARCHITECTURES = X64` | 只編譯 64-bit x86 版本 |
| `BUILD_TARGETS = DEBUG\|RELEASE` | 支援 DEBUG 和 RELEASE 兩種建置模式 |
| `[LibraryClasses]` | 每一行格式為 `LibraryName\|實作的.inf路徑`，將抽象 Library 名稱對應到實際實作 |
| `[Components]` | 列出要編譯的模組 .inf 路徑 |

> **重要觀念：** EDK2 採用「明確宣告制」，每個用到的 Library 都必須在 .dsc 裡指定實作路徑，不會自動繼承。這是常見的 build error 來源。

---

## 四、編譯指令

```bash
cd ~/edk2
source edksetup.sh               # 設定 EDK2 環境變數（每次開新終端機都要執行）
build -p MyPkg/MyPkg.dsc -a X64 -t GCC
```

| 參數 | 說明 |
|------|------|
| `-p MyPkg/MyPkg.dsc` | 指定要編譯的 Platform（.dsc 檔） |
| `-a X64` | 目標架構為 64-bit x86 |
| `-t GCC` | 使用 GCC 工具鏈（新版 EDK2 已移除 GCC5，改為 GCC） |

---

## 五、執行步驟

### 建立虛擬磁碟

```bash
dd if=/dev/zero of=~/uefi_disk.img bs=1M count=64   # 建立 64MB 空白磁碟
mkfs.vfat -F 32 ~/uefi_disk.img                     # 格式化為 FAT32
sudo mount ~/uefi_disk.img /tmp/uefi_mnt             # 掛載
sudo cp ~/edk2/Build/MyPkg/DEBUG_GCC/X64/MyHello.efi /tmp/uefi_mnt/
sudo umount /tmp/uefi_mnt                            # 卸載
```

> UEFI 只能讀取 FAT/FAT32 格式的磁碟，這是規範要求。

### 啟動 QEMU

```bash
qemu-system-x86_64 \
  -bios ~/edk2/Build/OvmfX64/DEBUG_GCC/FV/OVMF.fd \
  -drive format=raw,file=$HOME/uefi_disk.img \
  -nographic \
  -net none
```

| 參數 | 說明 |
|------|------|
| `-bios OVMF.fd` | 使用我們自己編譯的 OVMF 作為韌體 |
| `-drive format=raw,file=...` | 掛載虛擬磁碟（注意路徑要用 `$HOME` 而非 `~`） |
| `-nographic` | 不開視窗，輸出導向終端機 |
| `-net none` | 不使用網路 |

### 在 UEFI Shell 執行

```
map -r        # 掃描並列出所有磁碟 mapping
fs0:          # 切換到第一個檔案系統
ls            # 列出檔案
MyHello.efi   # 執行程式
```

---

## 六、今天遇到的錯誤與解法

| 錯誤 | 原因 | 解法 |
|------|------|------|
| `Tool chain [GCC5] is not defined` | 新版 EDK2 已移除 GCC5 | 改用 `-t GCC` |
| Build 0 秒就失敗、無錯誤訊息 | `MyPkg.dsc` 放在錯誤目錄 | 移到 `MyPkg/MyPkg.dsc` |
| `Linrary/UefiLib.h`、`EFISTATUS`、`EFI_SUCESS` | 打字錯誤 | 修正為正確拼法 |
| `MemoryAllocationLib not found` | .dsc 缺少 Library 宣告 | 補上對應的 .inf 路徑 |
| `StackCheckLib not found` | 同上 | 補上 `StackCheckLibNull` |
| `Could not open '~/uefi_disk.img'` | `~` 在 QEMU 參數中不展開 | 改用 `$HOME` |

---

## 七、學到的核心觀念

1. **UEFI 程式的三層結構：** `.c` 寫邏輯 → `.inf` 描述模組 → `.dsc` 組合整個 Package
2. **Library 明確宣告制：** 用到的每個 Library 都要在 `.dsc` 裡指定實作
3. **Unicode 字串：** UEFI 內部全用 Unicode，字串前要加 `L`
4. **FAT32 是 UEFI 的標準檔案系統**
5. **OVMF = UEFI 的虛擬機版本**，讓我們不需要真實硬體就能開發測試
